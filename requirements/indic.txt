# envs/indic venv -- Hindi primary model. Isolated from chatterbox.txt and
# xtts.txt because parler-tts pins its own transformers version, which has
# historically conflicted with Coqui TTS's pin.
#
# numpy<2: see requirements.txt -- numba (transitive) doesn't support
# NumPy 2.x yet.
torch
torchaudio
soundfile
pyyaml
numpy<2
transformers
parler-tts
