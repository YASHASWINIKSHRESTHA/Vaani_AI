# Orchestrator env: router dispatch, gate.py, similarity.py, round-trip ASR
# (both Whisper large-v3 and IndicWhisper), and everything in eval/. Does
# NOT include any TTS library -- those run in their own venvs (see
# requirements/chatterbox.txt, xtts.txt, indic.txt) because Coqui TTS,
# Chatterbox, and transformers-based Parler-TTS commonly pin incompatible
# torch/transformers versions against each other. See GAPS.md section 1b,
# item 6, and README "Environment setup" for how these venvs get used.
# numpy<2: numba (transitive, via openai-whisper/librosa) doesn't support
# NumPy 2.x yet. transformers<5: kept below the major bump that broke
# coqui-tts (see requirements.txt for the full note) for consistency, even
# though this env has no TTS library itself.
torch
torchaudio
pyyaml
soundfile
jiwer
speechbrain
openai-whisper
numpy<2
transformers>=4.57,<5
